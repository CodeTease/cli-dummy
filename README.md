# cli-dummy
For deployment testing purpose

About the CLI, it's a simple CLI tool that prints literally anything (that I can think of).

## Usage

```bash
cli-dummy
```

The main point is to test the release workflow.